# kitchensink

High-volume contapila project for **scale testing** (hundreds of thousands to ~1M transactions).

For a readable multi-ledger dogfood project with deep account structure, use **`../example/`** instead.

```bash
cd testdata/kitchensink
contapila status
contapila check
```
